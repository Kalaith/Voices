{
    "name": "voice-generator/backend",
    "description": "PHP backend for voice generator application",
    "type": "project",
    "require": {
        "php": ">=8.0",
        "vlucas/phpdotenv": "^5.4",
        "firebase/php-jwt": "^6.3"
    },
    "autoload": {
        "psr-4": {
            "VoiceGenerator\\": "src/"
        }
    },
    "scripts": {
        "start": "php -S localhost:8000 -t public/"
    }
}